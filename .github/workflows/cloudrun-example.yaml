on:
  push:
    branches:
      - example-build-deploy

name: Build and Deploy a Container
env:
  PROJECT_ID: gintaa-cloud-poc
  SERVICE: hello-cloud-run
  REGION: us-central1

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup Cloud SDK
      uses: 'google-github-actions/auth@v0.4.1'
      with:
        workload_identity_provider: 'projects/950612098384/locations/global/workloadIdentityPools/gintaa-wi-pool-poc/gintaa-wi-provider-poc'
        service_account: 'example-cloudrun@gintaa-cloud-poc.iam.gserviceaccount.com'

#     - name: Authorize Docker push
#       run: gcloud auth configure-docker

#     - name: Build and Push Container
#       run: |-
#         docker build -t gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE }}:${{  github.sha }} example-app/
#         docker push gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE }}:${{  github.sha }}
        
#     - name: Deploy to Cloud Run
#       id: deploy
#       uses: google-github-actions/deploy-cloudrun@v0.4.0
#       with:
#         service: ${{ env.SERVICE }}
#         image: gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE }}:${{  github.sha }}
#         region: ${{ env.REGION }}

#     - name: Show Output
#       run: echo ${{ steps.deploy.outputs.url }}
